---
plugin: google.cloud.gcp_compute
projects:
  - claude-code-00
auth_kind: application
filters:
  - labels.app = openclaw
  - labels.environment = production
  - status = RUNNING
keyed_groups:
  - key: zone
    prefix: zone
hostnames:
  - name
compose:
  ansible_host: name
