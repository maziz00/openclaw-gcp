---
gcp_secret_prefix: "openclaw"
secrets_dir: "/opt/openclaw/secrets"

# Bot token secret names — must match names created by Terraform
telegram_secret_name: "openclaw-production-telegram-bot-token"
discord_secret_name: "openclaw-production-discord-bot-token"
