{
  "agents": {
    "defaults": {
      "model": {
{% if claude_provider == "vertex_ai" %}
        "primary": "google-vertex/{{ openclaw_model }}"
{% else %}
        "primary": "anthropic/{{ openclaw_model }}"
{% endif %}
      }
    }
  }
}
